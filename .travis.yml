---
language: node_js
node_js:
  - "4.4.4"
sudo: false
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
cache:
  directories:
    - node_modules

before_install:
  - npm install -g ember-cli
  - ember -v
  - npm install -g bower
  - npm install -g forever
  - "npm config set spin false"

install:
  - npm install . -g
  - mkdir secondtest
  - cd secondtest
  - koaton new testapp
  - cd testapp
  - ls
  - ember new restapp -dir /home/travis/build/gerard2p/koaton/secondtest/testapp/ember/restapp
  - koaton ember restapp -nf

script:
  - npm test
