import CTABLE from 'ember-cli-crudtable/mixins/crud-controller';
import Ember from 'ember';
export default Ember.Controller.extend(CTABLE('{{model}}'), {
	actions: {},
	fieldDefinition: {
		{{definition}}
	}
});
